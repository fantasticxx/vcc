/*
/**************************************************
 *                                                *
 *   Program: vc-A2-T01.vc                        *
 *                                                *
 *   Vanilla-C compiler test program              *
 *   Purpose: Testing Vanilla-C compiler of A2    *
 *   Test Items:                                  *
 *     (1) Basic program unit                     *
 *     (2) Comments                               *
 *   Date: 2016/06/18                             *
 **************************************************
*/
main()
{
 /************************************************/

  // ----------------------------------------------
  //   DECLARATION: SYMBOLIC CONSTANTS & VARIABLES
  // ----------------------------------------------
    int x = 12;
    bool t1 = ((x > 10) && 
        (x < 20) && 
        ((x % 2 == 0) || (x % 3 == 0)) && 
        (x != 15));

    bool a = True, b = False, c = True, d = False;
    int y = 5, z = 10;

    bool t2 = (((a && b) || (c && d)) && 
        ((y == z)) && 
        ((y > 0) || (z < 20)));


    int p = 25, q = 10;
    bool isvalid = True;

    bool t3 = ((isvalid) && 
        ((p * q > 50) || (p + q < 100)) && 
        (p % 3 == 0) && 
        ((q == 0)));

    int m = 7, n = 0;
    bool t4 = (
        ((m > 0) ^ (n > 0)) && 
        (((m - n) > 5) || (m + n > 10))
    );

    bool t5 = 0 && 0 || 0 && 0 || 0 || 0 || (0 && 1 && 2 && 3) || (0 || 0);
    int t6 =  1 || 0 && 1 || 0 && 1 || 0;
    int t7 = -(((10 / 2) - 3) * 4);
    bool t8 = 'a' || 'b' && 1 || 0 && 1 || 123;
    int t9 = (20 / (4 + 1)) * (-2);
    const t10 = -(-(-10));
    const str = "Hi";
  // ----------------------------------------------
  //   PROCESSING STATEMENTS
  // ----------------------------------------------

 /************************************************/

 // --- END OF PROGRAM ----------------------------
}